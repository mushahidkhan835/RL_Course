- `data_preprocessing.py`is used to load the generated dataset and then pre-process it (reshaping images and cleaning .txt parts). 

- `data_splitting.py`is used to split the .jsonl dataset file into a train (80%), val (10%), and test (10%) set. 



